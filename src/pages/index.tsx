import { Layout } from '@/components/layout/Layout';

import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <Layout>
      <h1>非同期処理</h1>
    </Layout>
  );
};

export default Home;
